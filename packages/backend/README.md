# @leitor-ebook-ia/backend

API NestJS do leitor de ebooks com IA.

## Desenvolvimento

```bash
pnpm dev
```

Porta padrão: **3026** (via `EnvService`, sobrescrevível com `PORT`).

- Swagger: http://localhost:3026/api
- Health: http://localhost:3026/health
